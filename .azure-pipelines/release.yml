trigger:
  branches:
    exclude:
      - "*"
  tags:
    include:
      - "3.*"
      - "4.*"

pr: none

stages:
  - stage: Test
    jobs:
      - template: templates/integration-jobs.yml
  - stage: Release
    jobs:
      - template: templates/release-jobs.yml
